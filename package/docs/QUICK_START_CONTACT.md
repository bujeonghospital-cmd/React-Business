# 🚀 Quick Start Guide - Contact Inquiry Page

## ⚡ เริ่มใช้งานภายใน 5 นาที

### ขั้นตอนที่ 1: ตั้งค่า Google Maps API (2 นาที)

1. เปิด [Google Cloud Console](https://console.cloud.google.com/)

2. คลิก **Select a project** → **NEW PROJECT**

3. ตั้งชื่อ "TPP-Website" → **Create**

4. ไปที่ [APIs & Services](https://console.cloud.google.com/apis/dashboard)

5. คลิก **+ ENABLE APIS AND SERVICES**

6. ค้นหา "Maps Embed API" → เลือก → **ENABLE**

7. ไปที่ [Credentials](https://console.cloud.google.com/apis/credentials)

8. คลิก **+ CREATE CREDENTIALS** → **API key**

9. คัดลอก API Key ที่ได้

### ขั้นตอนที่ 2: ตั้งค่าโปรเจกต์ (1 นาที)

1. สร้างไฟล์ `.env.local` ใน root folder:

```bash
# Windows (PowerShell)
New-Item -Path .env.local -ItemType File
```

2. เปิดไฟล์และใส่ API Key:

```env
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=paste_your_api_key_here
```

3. **Save** และปิดไฟล์

### ขั้นตอนที่ 3: อัพเดทพิกัด (2 นาที)

1. เปิด [Google Maps](https://www.google.com/maps)

2. **สำหรับสำนักงานใหญ่**: ค้นหา "55/5 หมู่ 5 ถนนพหลโยธิน คลองหลวง ปทุมธานี"

3. คลิกขวาที่ตำแหน่งบนแผนที่ → คลิกตัวเลขพิกัด (คัดลอกอัตโนมัติ)

4. เปิดไฟล์ `src/app/contact-inquiry/page.tsx`

5. ค้นหา `headquarters` และแก้ไข:

```typescript
headquarters: {
  // ...
  coordinates: { lat: 13.9952, lng: 100.6405 }, // วางพิกัดที่คัดลอก
},
```

6. ทำซ้ำสำหรับโรงงาน (ค้นหา "9/9 หมู่ 6 กิ่งแก้ว บางพลี สมุทรปราการ")

7. **Save** ไฟล์

### ขั้นตอนที่ 4: ทดสอบ (30 วินาที)

```bash
# Restart dev server
npm run dev
```

เปิดเบราว์เซอร์: `http://localhost:3000/contact-inquiry`

---

## ✅ เช็คลิสต์ก่อนใช้งาน

- [ ] ไฟล์ `.env.local` ถูกสร้างแล้ว
- [ ] API Key ถูกใส่ใน `.env.local`
- [ ] พิกัดสำนักงานถูกอัพเดท
- [ ] พิกัดโรงงานถูกอัพเดท
- [ ] Dev server ถูก restart แล้ว
- [ ] หน้าเว็บแสดงผลถูกต้อง
- [ ] แผนที่แสดงถูกต้อง
- [ ] ฟอร์มส่งได้

---

## 🎯 ทดสอบฟังก์ชัน

### 1. ทดสอบแผนที่

- [ ] แผนที่สำนักงานใหญ่แสดงถูกต้อง
- [ ] แผนที่โรงงานแสดงถูกต้อง
- [ ] สลับระหว่าง tab ได้
- [ ] ลิงก์ "ดูเส้นทางใน Google Maps" ทำงาน

### 2. ทดสอบฟอร์ม

- [ ] กรอกข้อมูลไม่ครบ → แสดง error
- [ ] กรอกอีเมลผิดรูปแบบ → แสดง error
- [ ] กรอกครบถ้วน → ส่งได้สำเร็จ
- [ ] แสดง Success message
- [ ] ฟอร์มถูก reset หลังส่งสำเร็จ

### 3. ทดสอบ Responsive

- [ ] Desktop (> 1024px) - 2 columns
- [ ] Tablet (640-1024px) - 1 column wide
- [ ] Mobile (< 640px) - 1 column narrow

---

## 🔧 การแก้ไขปัญหาด่วน

### แผนที่ไม่แสดง

**สาเหตุที่พบบ่อย:**

1. ❌ API Key ไม่ถูกต้อง
2. ❌ ยังไม่ Enable Maps Embed API
3. ❌ ยังไม่ restart dev server

**วิธีแก้:**

```bash
# 1. ตรวจสอบ .env.local
cat .env.local

# 2. ตรวจสอบว่ามี API Key หรือไม่
echo $env:NEXT_PUBLIC_GOOGLE_MAPS_API_KEY  # Windows PowerShell

# 3. Restart dev server (จำเป็น!)
# Ctrl+C แล้วรันใหม่
npm run dev
```

### ฟอร์มส่งไม่ได้

**สาเหตุที่พบบ่อย:**

1. ❌ API route ยังไม่ถูกสร้าง
2. ❌ Network error

**วิธีแก้:**

1. เช็คว่ามีไฟล์ `src/app/api/contact/route.ts` หรือไม่
2. เปิด Developer Console (F12) → Network tab
3. กรอกฟอร์มและส่ง
4. ดู request ไปที่ `/api/contact` หรือไม่
5. ดู response status (ควรเป็น 200)

### พิกัดไม่ถูกต้อง

**วิธีแก้:**

1. ตรวจสอบรูปแบบ: `{ lat: 13.9952, lng: 100.6405 }`
2. ห้ามใส่ string: `{ lat: "13.9952", lng: "100.6405 }` ❌
3. ใช้จุดทศนิยม ไม่ใช่จุลภาค

---

## 📱 ทดสอบบน Mobile จริง

### Option 1: ใช้ ngrok (แนะนำ)

```bash
# ติดตั้ง ngrok
# ดาวน์โหลดจาก: https://ngrok.com/download

# รัน ngrok
ngrok http 3000
```

จะได้ URL เช่น `https://xxxx-xxx-xxx.ngrok.io`

เปิด URL นี้บนมือถือ → ทดสอบได้เลย

### Option 2: ใช้ IP Address

```bash
# หา IP Address ของเครื่อง
ipconfig  # Windows
```

แก้ไข `package.json`:

```json
{
  "scripts": {
    "dev": "next dev -H 0.0.0.0"
  }
}
```

เปิดบนมือถือ: `http://your-ip:3000/contact-inquiry`

---

## 🎨 Customization ด่วน

### เปลี่ยนสี Theme

ค้นหาและแทนที่ใน `page.tsx`:

```typescript
// จาก
className = "bg-gradient-to-r from-blue-600 to-emerald-600";

// เป็น
className = "bg-gradient-to-r from-red-600 to-orange-600"; // แดง-ส้ม
className = "bg-gradient-to-r from-purple-600 to-pink-600"; // ม่วง-ชมพู
className = "bg-gradient-to-r from-green-600 to-teal-600"; // เขียว-ฟ้า
```

### แก้ไขข้อความ

ค้นหาและแก้ไขใน `COMPANY_INFO`:

```typescript
const COMPANY_INFO = {
  name: "ชื่อบริษัทของคุณ", // แก้ได้
  // ...
  workingHours: {
    weekdays: "จันทร์-ศุกร์: 9:00-18:00 น.", // แก้ได้
    // ...
  },
};
```

---

## 📚 เอกสารเพิ่มเติม

- 📖 คู่มือฉบับเต็ม: `docs/CONTACT_INQUIRY_README.md`
- 🗺️ วิธีตั้งค่า Google Maps: `docs/GOOGLE_MAPS_SETUP.md`
- 📊 สรุปโปรเจกต์: `docs/CONTACT_INQUIRY_SUMMARY.md`
- 🎨 UI Preview: `docs/CONTACT_INQUIRY_UI_PREVIEW.md`

---

## 🆘 ต้องการความช่วยเหลุอเพิ่มเติม?

### เช็คข้อผิดพลาดทั่วไป

```bash
# 1. เช็ค console logs
# เปิด Developer Tools (F12)
# ดูที่ Console tab

# 2. เช็ค Network requests
# ดูที่ Network tab
# กรอกฟอร์มและส่ง
# ดู request/response

# 3. เช็ค Server logs
# ดู Terminal ที่รัน npm run dev
```

### ติดปัญหา API Key?

ลองใช้ OpenStreetMap แทน (ฟรี):

แก้ไข iframe ใน `page.tsx`:

```typescript
<iframe
  src={`https://www.openstreetmap.org/export/embed.html?bbox=${
    currentLocation.coordinates.lng - 0.01
  },${currentLocation.coordinates.lat - 0.01},${
    currentLocation.coordinates.lng + 0.01
  },${currentLocation.coordinates.lat + 0.01}&layer=mapnik&marker=${
    currentLocation.coordinates.lat
  },${currentLocation.coordinates.lng}`}
  // ... rest of props
/>
```

---

## ✨ เสร็จแล้ว!

หน้า Contact Inquiry พร้อมใช้งาน 🎉

**Next Steps:**

1. ⭐ ตั้งค่าการส่งอีเมล (ดูใน `route.ts`)
2. ⭐ Deploy ขึ้น production
3. ⭐ เพิ่ม reCAPTCHA (ป้องกัน spam)

---

**เวลารวมที่ใช้:** ~5 นาที  
**ระดับความยาก:** ⭐⭐☆☆☆ (ง่าย)  
**Status:** ✅ Ready to Use
